<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - SecurityCar</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Inter', sans-serif;
        }
        .main-container {
            display: flex;
            min-height: 100vh;
        }
        .branding-section {
            background: linear-gradient(rgba(0, 0, 0, 0.65), rgba(0, 0, 0, 0.65)), url('https://images.pexels.com/photos/164634/pexels-photo-164634.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=1');
            background-size: cover;
            background-position: center;
        }
        .form-section {
            background-color: #f8f9fa;
        }
    </style>
</head>
<body>
    <div class="container-fluid p-0 main-container">
        <div class="row g-0 w-100">
            <!-- Coluna da Esquerda: Branding -->
            <div class="col-lg-7 d-none d-lg-flex flex-column justify-content-center align-items-center text-white branding-section">
                <!-- LOGO ATUALIZADA -->
                <h1 class="display-4 fw-bold">SecurityCar</h1>
                <p class="lead">Segurança e controlo para o seu estacionamento.</p>
            </div>

            <!-- Coluna da Direita: Formulário -->
            <div class="col-lg-5 d-flex flex-column justify-content-center align-items-center form-section">
                <div class="card shadow-sm border-0" style="width: 24rem;">
                    <div class="card-body p-5">
                        <h3 class="card-title text-center mb-4">Acesso Restrito</h3>
                        <form action="/login" method="POST">
                            <div class="form-floating mb-3">
                                <input type="text" class="form-control" id="username" name="username" placeholder="Usuário" required>
                                <label for="username"><i class="bi bi-person me-2"></i>Utilizador</label>
                            </div>
                            <div class="form-floating mb-4">
                                <input type="password" class="form-control" id="password" name="password" placeholder="Senha" required>
                                <label for="password"><i class="bi bi-lock me-2"></i>Palavra-passe</label>
                            </div>
                            
                            <% if (typeof error !== 'undefined' && error) { %>
                                <div class="alert alert-danger d-flex align-items-center" role="alert">
                                    <i class="bi bi-exclamation-triangle-fill me-2"></i>
                                    <div>
                                        <%= error %>
                                    </div>
                                </div>
                            <% } %>
                            
                            <div class="d-grid mt-4">
                                <button type="submit" class="btn btn-primary btn-lg">Entrar</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>

